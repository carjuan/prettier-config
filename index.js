/**
 * @type {import("prettier"(.Config)}
 */
const config = {
  tabWidth: 4,
  semi: true,
  singleQuote: true,
  arrowParens: "always",
  printWidth: 120,
  quoteProps: "as-needed",
};

export default config;

/**
 * @type {import("prettier"(.Config)}
 */
const config = {
  tabWidth: 4,
  semi: true,
  singleQuote: true,
  arrowParens: "always",
  quoteProps: "as-needed",
  proseWrap: "always",
};

export default config;
